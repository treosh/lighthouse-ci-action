{
  "name": "lighthouse-ci-action",
  "version": "1.0.1",
  "license": "MIT",
  "description": "Run Lighthouse in CI using Github Actions",
  "repository": "treosh/lighthouse-ci-action",
  "engines": {
    "node": "10"
  },
  "scripts": {
    "test": "prettier -c src/** package.json README.md && tsc -p ."
  },
  "dependencies": {
    "@actions/core": "^1.1.1",
    "@lhci/cli": "0.3.0-alpha.0"
  },
  "devDependencies": {
    "@types/fs-extra": "^8.0.0",
    "@types/node": "^12.7.5",
    "prettier": "^1.18.2",
    "typescript": "^3.6.3"
  }
}
